language: node_js
node_js:
- 6.10.3
cache: npm

install:
  - npm ci
script:
  - npm start
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: gh-pages